import { createHeightEntry } from '../Components/HeightHOC'
import HeightEntry from '../Components/HeightEntry'

const HeightEntryScreen = createHeightEntry(HeightEntry)

export default HeightEntryScreen